# V26.1 DASHBOARD UI LIBRARY

> **Version:** 26.1.0  
> **Letzte Aktualisierung:** 2025-01-26  
> **Status:** HERO-QUALITÄT ERREICHT ✨

---

## 🎯 ÜBERSICHT

Alle Dashboard-UI-Elemente in Hero-Qualität für systemweite Wiederverwendung.

---

## 📚 KOMPONENTEN-KATALOG

### 1. **V26IconBox**

**Pfad:** `src/components/design-system/V26IconBox.tsx`  
**Zweck:** Premium Icon Container mit Glow

#### Props:

```typescript
interface V26IconBoxProps {
  icon: LucideIcon;
  size?: "sm" | "md" | "lg";
  className?: string;
}
```

#### Sizes:

- `sm`: 40x40px (Icon: 20px) - Für kompakte Bereiche
- `md`: 48x48px (Icon: 24px) - Standard
- `lg`: 64x64px (Icon: 32px) - Headers, Hero

#### Styling:

- Background: `KERNFARBEN.dunkelblau`
- Icon Color: `KERNFARBEN.beige`
- Border: 2px solid dunkelblau
- Glow: `0 0 15px rgba(234, 222, 189, 0.19)`

#### Verwendung:

```tsx
import { V26IconBox } from "@/components/design-system/V26IconBox";
import { Clock } from "lucide-react";

<V26IconBox icon={Clock} size="sm" />;
```

---

### 2. **V26PerformanceBadge**

**Pfad:** `src/components/design-system/V26PerformanceBadge.tsx`  
**Zweck:** Trend-Badges (+12.5%, Zahlen, etc.)

#### Props:

```typescript
interface V26PerformanceBadgeProps {
  value: string;
  trend?: "up" | "down" | "neutral";
  showIcon?: boolean;
  size?: "sm" | "md" | "lg";
  className?: string;
}
```

#### Sizes:

- `sm`: 10px Text, 12px Icon
- `md`: 12px Text, 14px Icon (Standard)
- `lg`: 14px Text, 16px Icon

#### Styling:

- Background: `rgba(234, 222, 189, 0.15)` (Beige transparent)
- Text Color: `KERNFARBEN.dunkelblau`
- Border: 2px solid `rgba(234, 222, 189, 0.3)`
- Glow: `0 0 12px rgba(234, 222, 189, 0.12)`

#### Verwendung:

```tsx
import { V26PerformanceBadge } from '@/components/design-system/V26PerformanceBadge';

<V26PerformanceBadge value="+12.5%" trend="up" />
<V26PerformanceBadge value="142" showIcon={false} />
```

---

### 3. **DashboardKPICard**

**Pfad:** `src/components/hero/DashboardKPICard.tsx`  
**Zweck:** KPI Cards für Dashboard Preview

#### Props:

```typescript
interface DashboardKPICardProps {
  icon: LucideIcon;
  label: string;
  value: string;
  trend?: string;
}
```

#### Styling:

- Background: `KERNFARBEN.weiss`
- Border: 2px solid rgba(229, 231, 235, 0.5)
- Hover Border: `rgba(234, 222, 189, 0.37)`
- Glow: `0 0 15px rgba(50, 61, 94, 0.05)`

#### Verwendung:

```tsx
import { DashboardKPICard } from "@/components/hero/DashboardKPICard";
import { FileText } from "lucide-react";

<DashboardKPICard icon={FileText} label="Aufträge" value="142" trend="+12%" />;
```

---

### 4. **DashboardInfoPanel**

**Pfad:** `src/components/dashboard/DashboardInfoPanel.tsx`  
**Zweck:** Info-Leiste am unteren Bildschirmrand

#### Features:

- ✅ Premium Dunkelblau Background
- ✅ Glassmorphism (backdrop-filter: blur(16px))
- ✅ V26IconBox für alle Icons
- ✅ V26PerformanceBadge für Zahlen
- ✅ Synchron zur Sidebar (bewegt sich mit)

#### Content Sections:

1. Uhrzeit & Datum (Clock Icon)
2. Fahrzeugstatus (Activity Icon)
3. Legende (MapPin Icon)
4. Wetter
5. Verkehrslage
6. Aufträge (FileText Icon)

#### Verwendung:

```tsx
import { DashboardInfoPanel } from "@/components/dashboard/DashboardInfoPanel";

<DashboardInfoPanel sidebarExpanded={sidebarExpanded} />;
```

---

### 5. **DashboardSidebar**

**Pfad:** `src/components/dashboard/DashboardSidebar.tsx`  
**Zweck:** Info-Board rechts neben Sidebar

#### Features:

- ✅ Premium Dunkelblau Background
- ✅ Glassmorphism
- ✅ Beige Akzente
- ✅ Icon-Boxen für alle Sektionen
- ✅ Badges für Status

#### Content Sections:

1. Auftrags-Übersicht (Header)
2. Neue Kunden (7 Tage)
3. Rechnungs-Status
4. Heutige Aufträge
5. Letzte 10 Aufträge (scrollbar)
6. Dringende Aktionen (conditional)

#### Verwendung:

```tsx
import { DashboardSidebar } from "@/components/dashboard/DashboardSidebar";

<DashboardSidebar sidebarExpanded={sidebarExpanded} />;
```

---

## 🎨 DESIGN TOKENS USAGE

### Farben (KERNFARBEN):

```typescript
import { KERNFARBEN } from "@/lib/design-system/pricing-colors";

colors.dunkelblau; // #323D5E - Primary Background
colors.beige; // #EADEBD - Accent/Icons
colors.weiss; // #FFFFFF - Text/Cards
colors.canvas; // #F8F9FB - Light Background
```

### V26.1 Tokens:

```typescript
import { DESIGN_TOKENS_V26_1 } from "@/lib/design-system/v26-1-tokens";

// Colors
colors.dunkelblau;
colors.beige;
colors.weiss;
colors.status_success;
colors.status_warning;
colors.status_error;

// Elevation
elevation.sm;
elevation.md;
elevation.lg;
elevation.xl;
elevation["2xl"];

// Motion
motion.duration.fast; // 150ms
motion.duration.normal; // 300ms
motion.duration.slow; // 500ms
```

---

## 🔧 INTEGRATION BEISPIEL

### Dashboard mit allen Komponenten:

```tsx
import { DashboardLayout } from "@/components/layout/DashboardLayout";
import { DashboardInfoPanel } from "@/components/dashboard/DashboardInfoPanel";
import { DashboardSidebar } from "@/components/dashboard/DashboardSidebar";
import { HEREMapComponent } from "@/components/dashboard/HEREMapComponent";

export default function Dashboard() {
  const [sidebarExpanded, setSidebarExpanded] = useState(true);

  return (
    <DashboardLayout>
      {/* Sidebar mit Info-Board */}
      <DashboardSidebar sidebarExpanded={sidebarExpanded} />

      {/* Map mit Premium White Border */}
      <div className="flex-1 relative p-4">
        <div
          className="h-full rounded-2xl overflow-hidden"
          style={{
            backgroundColor: "#FFFFFF",
            border: "3px solid",
            borderColor: "rgba(234, 222, 189, 0.25)",
            boxShadow: "0 0 40px rgba(234, 222, 189, 0.15)",
          }}
        >
          <HEREMapComponent />
        </div>
      </div>

      {/* Info Panel am Footer */}
      <DashboardInfoPanel sidebarExpanded={sidebarExpanded} />
    </DashboardLayout>
  );
}
```

---

## ✅ QUALITY GATES

### Vor Wiederverwendung prüfen:

- [ ] Komponente nutzt V26IconBox für Icons
- [ ] Komponente nutzt V26PerformanceBadge für Zahlen/Trends
- [ ] Komponente nutzt KERNFARBEN (keine direkten Hex-Codes)
- [ ] Komponente hat Glow/Shadow Effects
- [ ] Komponente hat Glassmorphism (backdrop-filter)
- [ ] Komponente ist responsive
- [ ] Komponente folgt font-sans

---

**Status:** Alle Dashboard-UI-Elemente sind in Hero-Qualität und ready for systemweite Verwendung! 🎉
